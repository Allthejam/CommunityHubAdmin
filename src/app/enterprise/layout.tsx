/**
 * NEUTRALIZED LAYOUT
 * This part of the application is redundant for the Admin Hub.
 */
export default function RedundantLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
